# Personal LLM

Personal LLM is the reusable core of the AI ecosystem: memory, RAG, a model router,
an agent loop, and a knowledge graph. Zaid built it first so later projects import it
instead of rebuilding retrieval and routing.

Second Brain builds directly on Personal LLM, adding vault ingestion and auto-linking.
